version: 2

sphinx:
  configuration: docs/conf.py

formats: []

build:
  image: latest

python:
  version: 3.7
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs
    - method: pip
      path: cftime==1.4.1
